package leaksnow

// Temporary stubs — full method sets are added in later tasks.
// DELETE this file when shodan.go, ulp.go, and intelx.go are added,
// as those files will redeclare these structs.
type ShodanService struct{ client *Client }
type ULPService struct{ client *Client }
type IntelXService struct{ client *Client }
